
  # Design Login Screen

  This is a code bundle for Design Login Screen. The original project is available at https://www.figma.com/design/JgxxTpxuprErIP9MsY5Mod/Design-Login-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  